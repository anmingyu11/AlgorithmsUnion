## NQueens

#### 有关当前位置是否合法的判断
1. 判断四个方向,假设当前位置为`(x,y)`,判断位置为`(i,j)`
2. 确定定义域为,横轴`(0,y)`因为深搜的特性,当前位置的右侧没有填数,纵轴`(0,n)`
3. 主对角线方向,`x - i == y - j && i - x == j - y`合并起来就是`x - y == i - j`
4. 副对角线方向, `x - i == j - y`